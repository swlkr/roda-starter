h1 'sign up'

form method: 'post', action: signup_path do
  div { csrf_tag signup_path }

  label for: 'email' do 'Enter your email to get your login link' end
  input type: 'email', name: 'email', autofocus: true, placeholder: 'you@email.com'

  input type: 'submit', value: 'Sign up for free'

  a href: login_path do
    'Already have an account? Login here'
  end
end
